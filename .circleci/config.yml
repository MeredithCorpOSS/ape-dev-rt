version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:1.13.5
    working_directory: /go/src/github.com/TimeIncOSS/ape-dev-rt
    steps:
      - checkout
      - run: wget https://releases.hashicorp.com/terraform/0.11.14/terraform_0.11.14_linux_386.zip
      - run: sudo unzip terraform_0.11.14_linux_386.zip -d /usr/local/bin
      - run:
          name: install deps
          command: |
             make installdeps
      - run:
          name: run test
          command: |
             make test

workflows:
  version: 2
  test-workflow:
    jobs:
      - test:
          filters:
            tags:
              only: /.*/
